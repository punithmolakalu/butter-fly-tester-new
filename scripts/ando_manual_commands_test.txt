# Manual Ando query list — one command per line.
# Lines starting with # are ignored. Blank lines are ignored.
#
# Run (adjust --ando to your GPIB address):
#   python scripts/ando_commands_check.py --ando GPIB0::5::INSTR --no-defaults --file scripts/ando_manual_commands_test.txt
#
# Optional: add --sweep before queries if you need a fresh trace (WDATA?, LDATA?, ANA?, etc.)
#   python scripts/ando_commands_check.py --ando GPIB0::5::INSTR --no-defaults --file scripts/ando_manual_commands_test.txt --sweep
#
# Type commands directly in the terminal (responses print below each line):
#   python scripts/ando_manual_terminal.py --ando GPIB0::5::INSTR
#   python scripts/ando_commands_check.py --ando GPIB0::5::INSTR --manual
#
# Full list of queries parsed from the manual:
#   python scripts/ando_commands_check.py --list-md-queries
#
# --- Add your queries below (examples commented out; uncomment or replace) ---

*IDN?
# CTRWL?
# SPAN?
# SWEEP?
